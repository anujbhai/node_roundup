{
  "name": "registry-test",
  "version": "1.0.0",
  "description": "Test suite for student registry library",
  "main": "index.js",
  "scripts": {
    "setup": "cd .. && yarn build",
    "test": "rm -f registrardb.sqlite && mocha ./index.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^5.1.1",
    "mocha": "^10.7.3"
  }
}
